# 学习任务：

学习git和gitub的使用以及初步的机器学习

# 完成情况：

1.将群里的Git小游戏通关。掌握了git的很多基本用法
  最基础的命令git commit,git branch,get merge,git rebase
  更高级一些的命令，git commit -m:备注，将暂存区的内容提交到当前分支
                  git log:查看当前分支的所有版本
                  git refiog:查看HEAD指针的移动历史（包括被回滚的版本）
                  git stash:将工作区和缓存区中尚未提交的修改存入栈中
                  git stash apply:将栈项存储的修改恢复到当前分支，但不删除栈顶元素
                  git stash pop：将栈顶存储的修改恢复到当前分支，同时删除栈顶元素
                  git stash drop：删除栈顶存储的修改
                  git stash list：查看栈中所有元素

2、对于一些概念上内容也有了较深的理解，经过之前对于git的讲解，我对于操作流程有了更好的了解，对于工作区、暂存区、以及版本区有了跟清晰的认识
3、斯坦福视觉精讲，让我对于机器学习有了一个初步的了解，以及一些基础的命令了解（没有听很多，只听了一个半视频）

# 学习中的问题

学过的git的命令记不住，每次都是看到后知道学过但却忘记了，以及用的时候知道学过，但写不出来

#总结和思考

这几天事情比较多，没有投入很多时间来学习，所以在下一阶段要腾出更多的时间来学习，以及命令要背记，要多写，多用

